[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "relay_modbus_lib"
version = "0.1"
description = "Relay control library Waveshare 32 relays"
dependencies = []
